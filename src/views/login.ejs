<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - Movie Watchlist</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <main class="container">
      <div class="settings-container" style="margin-top: 4rem">
        <section class="profile-section">
          <div class="hero" style="padding: 1rem 0 2rem 0; margin-bottom: 2rem">
            <h1>Movie Watchlist</h1>
            <p>Please log in to continue</p>
          </div>

          <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-error" style="background-color: #fee; border: 1px solid #fcc; color: #c33; padding: 1rem; margin-bottom: 1rem; border-radius: 4px;">
              <%= error %>
            </div>
          <% } %>

          <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success" style="background-color: #efe; border: 1px solid #cfc; color: #363; padding: 1rem; margin-bottom: 1rem; border-radius: 4px;">
              <%= success %>
            </div>
          <% } %>

          <form
            id="loginForm"
            class="settings-form"
            method="POST"
            action="/users/login"
          >
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

            <div class="form-group">
              <label for="email">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                placeholder="name@example.com"
              />
            </div>

            <div class="form-group">
              <label for="password">Password</label>
              <input type="password" id="password" name="password" required />
            </div>

            <button type="submit" class="btn-primary">Login</button>
          </form>

          <p class="text-center mt-2">
            Don't have an account?
            <a href="/users/register">Register here</a>
          </p>
        </section>
      </div>
    </main>

    <footer style="margin-top: 3rem">
      <div class="container">
        <p>&copy; 2025 Movie Watchlist App. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
